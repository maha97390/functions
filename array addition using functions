#include<string.h>
int main()
{
    int arr1[4]={1,2,3,4};
    int arr2[4]={5,6,7,8};
    addarray(arr1,arr2);
    return 0;
}
int addarray(int a[],int b[])
{
    int c[4];
    for(int i=0;i<=4;i++)
    {
        {
            c[i]=a[i]+b[i];
            printf("%d ",c[i]);
        }
    }
}
